{
  "name": "identity-registry",
  "version": "1.0.0",
  "description": "Hyperledger Fabric chaincode for blockchain-based device identity registration and revocation",
  "main": "index.js",
  "scripts": {
    "start": "fabric-chaincode-node start",
    "test": "node --check index.js && node --check lib/authorization.js && node --check lib/auditState.js && node --check lib/IdentityRegistryContract.js && node --check lib/AccessControlContract.js && node --check lib/AuditLogContract.js && node --check test/accessControlContract.test.js && node test/accessControlContract.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.8",
    "fabric-shim": "^2.5.8"
  },
  "author": "Ruel Mumba",
  "license": "MIT"
}
